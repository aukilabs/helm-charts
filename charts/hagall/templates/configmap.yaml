apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hagall.fullname" . }}
  labels:
    {{- include "hagall.labels" . | nindent 4 }}
data:
  HAGALL_ADDR: :8080
  {{- toYaml .Values.config | nindent 2  }}
